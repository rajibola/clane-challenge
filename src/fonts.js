import {colors} from './colors';
import {h} from './../src/shared/resposive-dimension';

export const fonts = {
  font16: {
    textTransform: 'uppercase',
    fontSize: h(16),
    lineHeight: h(24),
    color: colors.dark,
  },
};
